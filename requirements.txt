# 指定 PyTorch CPU 专用源（关键！）
--extra-index-url https://download.pytorch.org/whl/cpu

# 核心依赖（使用 +cpu 后缀确保兼容）
streamlit>=1.28.0
torch==2.1.0+cpu
torchvision==0.16.0+cpu
ultralytics>=8.0.0
opencv-python>=4.7.0
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.5.0
Pillow>=8.3.0


# 额外依赖
pyyaml>=6.0
requests>=2.25.0
tqdm>=4.62.0
seaborn>=0.11.0